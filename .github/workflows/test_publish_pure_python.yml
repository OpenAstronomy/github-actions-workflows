on:
  push:
  pull_request:

jobs:
  release:
    uses: ConorMacBride/github-actions-workflows/publish_pure_python.yml@move-to-root
    with:
      test_extras: test
      test_command: pytest --pyargs test_package
